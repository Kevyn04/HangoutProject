// Point this at your running Spring Boot backend.
// Simulator/emulator: use localhost.
// Physical device on the same Wi-Fi: use your machine's local IP (e.g. 192.168.x.x).
export const API_BASE_URL = "http://localhost:8080";
